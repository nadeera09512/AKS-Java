name: Docker Image CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
        ls -l
        pwd
        whoami
        docker build . --file Dockerfile --tag aks-java:$(date +%s)

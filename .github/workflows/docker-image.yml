name: Docker Image CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Build and push the Docker image
      run: |
        docker login -u admin -p abc123 http://172.31.21.249:5000
        docker build . --file Dockerfile --tag 172.31.21.249:5000/aks-java:$(date +%s)
        docker push 172.31.21.249:5000/aks-java:$(date +%s)
